//
// Configuration file for the program training recog
//

config:
{

  compute_views_descriptors:
  {
    path_to_dataset = "../../../../Datasets/Dataset_cat10";
    
    extension_cloud_filename = ".ply";
    enable_esf = true;  // ESF feature descriptor
    enable_vfh = false;  // VFH feature descriptor
    enable_cvfh = false;  // CVFH feature descriptor
    enable_ourcvfh = false;  // OURCVFH feature descriptor
    enable_grsd = false;  // GRSD feature descriptor
    enable_spin = false;  // SPIN feature descriptor
    enable_usc = false;  // USC feature descriptor
    enable_sc = false;  // 3DSC feature descriptor
    enable_crh = false;  // CRH feature descriptor
    enable_good = false;  // CRH feature descriptor
    enable_gshot = false; // GSHOT feature descriptor
    enable_volume = false;  // Volume of the object
    enable_resolution = true; //If we want to compute the resolution and add resolution invariance to point cloud
    leaf_resolution = 0.01; //// For cloud resolution invariance, 0.01 for svm 
    xres = 150; // the size of the window (X) used to render the partial view of the object
    yres = 150; // the size of the window (Y) used to render the partial view of the object
    view_angle = 60; //  field of view of the virtual camera
    radius_sphere = 1.0; //  Radius of the sphere where the virtual camera will be placed
    model_scale = 1.0; // to scale model before rendering
    tesselation_level = 0.0; //represents the number of subdivisions applied to the triangles of original icosahedron
    save_view = true; //Save each view generated 
    save_binary = false; // Save file binary format. Otherwise it's ASCII
    enable_compute_views = true; //If true, perform virtual scanner, otherwise compute descriptors on the object directly
    concatenation_descriptors = 1; // 0 --> ESF and VFH, 1 --> ESF and GSHOT, 2 -->  ESF and GRSD, 3 -->  ESF, GRSD and GSHOT, 4 --> ESF, GRSD, GSHOT, VFH, 5 --> GRSD and GSHOT, 6 --> ESF, VFH, GSHOT

  }
  build_index:
  {
   

  }
  retrieval_similarity_search:
  {
  enable_resolution = true; //If we want to compute the resolution and add resolution invariance to point cloud
  leaf_resolution = 0.01; //// For cloud resolution invariance, 0.01 for svm 

  }

};
